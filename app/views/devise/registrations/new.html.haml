.single-container
  = render partial: 'items/items-header'
    
    
  %main.single-main
    .single-main__box
      .single-main__header
        %h2 会員情報入力
        
      .single-main__form
        .single-main__content
          = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
            = render "devise/shared/error_messages", resource: resource
            .field
              = f.label :名前
              %span.form-require 必須
                
              %br/
              = f.text_field :nickname, autofocus: true, autocomplete: "nickname", placeholder: 'ニックネーム', class: 'input-default'
                
            .field
              = f.label :メールアドレス
              %span.form-require
                必須
              %br/
              = f.email_field :email, autofocus: true, autocomplete: "email", placeholder: 'PC・携帯どちらでも可', class: 'input-default'
              .form-field
                .field
                  = f.label :パスワード
                  - if @minimum_password_length
                    %em

                  %br/
                  = f.password_field :password, autocomplete: "new-password", placeholder: '7文字以上の半角英数字', class: 'input-default'
                  %p.form-text
                    ※ 英字と数字の両方を含めて設定してください
                    %input#checkbox-id.custom-checkbox{ type: 'checkbox' }
                    %label{ for: 'checkbox-id' }               
                      パスワードを表示する
                      
              .field
                %h3.text-left.sub-head
                  本人確認
                %p.text-box
                  安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。

              .field
                = f.label :お名前（全角）
                
                %span.form-require
                  必須
                %br/
                = f.text_field :family_name, autocomplete: "family_name", placeholder: '例)山田', class: 'input-left'
                = f.text_field :family_name, autocomplete: "first_name", placeholder: '例)太郎', class: 'input-right'

              .field
                = f.label :お名前カナ（全角）
                
                %span.form-require
                  必須
                %br/
                = f.text_field :family_name, autocomplete: "family_name_kana", placeholder: '例)ヤマダ', class: 'input-left'
                = f.text_field :family_name, autocomplete: "first_name_kana", placeholder: '例)タロウ', class: 'input-right'


              .field
                = f.label :生年月日
                
                %span.form-require
                  必須
                %br/
                
                = f.text_field :birth_day, autocomplete: "birth_day", placeholder: '年/月/日', class: 'input-birthday'

                .form-info-text
                  ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。




              .single-content
                = f.submit '次へ進む', class: 'single-inner'


  %footer.single-footer
    %nav
      %ul.clearfix
        %li 
          = link_to 'プライバシーポリシー', "#"

        %li 
          = link_to 'フリマ利用規約', "#"

        %li 
          = link_to '特定商品取引に関する表記', "#"
    
          .footer-logo
            = link_to root_path do
              = image_tag "/material/logo/logo.png", class: "furima-image"
    
    .footer-name
      ©︎ FURIMA
